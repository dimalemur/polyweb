import React from 'react';
import { Authentication } from '../pages/authentication';
import './app.pcss';

export const App = (props) => {
    return (
        <div className='App' >
            <Authentication />
        </div>
    )
}